<section class ="s-block">
    <div class="container">
		<div class="flex flex-col justify-center items-center store-features bg-[#F8F4F4] rounded-xl p-5" style="background-color :  ; border-radius: 40px ; padding : 20px">
			<h1> {{component.title}}</h1>
			<div class="grid-feattures-about">
				{% for link in component.banner_payment %}
					<a href="{{link.url}}" class="flex flex-col justify-center items-center">
							<img src="{{link.image}}" class="w-full h-full" />
					</a>
				{% endfor %}
			</div>
		</div>
	</div>
</section>