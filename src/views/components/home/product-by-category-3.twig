<section class="s-block s-block-tabs s-block--special-products container tabs-initialized category-three">
    <div class="s-block__title">
        <div class="right-side">
            <h2 class="!text-3xl !font-extrabold relative after:absolute after:-bottom-4 after:right-0 after:w-full after:h-[10px] after:rounded-lg after:bg-[#404553]">{{ component.title}}</h2>
        </div>
        {{ component.subtitle }}
        </div>
            <span class="hidden">{{ component.category_id[0].dropdown_list.value[0]|json_encode() }}</span>
        <div>
    </div>
</section>