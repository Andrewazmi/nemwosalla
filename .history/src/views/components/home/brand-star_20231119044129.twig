<section class="s-block">
	<div class="container flex flex-col items-center justify-center store-features">
		<div class="w-5/12">
			<h1 class="mb-5 text-5xl text-gray-800">{{component.title}}</h1>
			<div class="w-full h-3 mb-8 bg-gray-600 rounded-lg"></div>
		</div>
		<div class="grid-feattures-about">
			{% for link in component.brand %}
				<div class="flex flex-col items-center justify-center ">
					<img src="{{link.image}}"/>
				</div>
			{% endfor %}
		</div>
	</div>
</section>
