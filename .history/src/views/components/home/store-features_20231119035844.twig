<section class="s-block">
	<div class="container flex flex-col items-center justify-center store-features">
		<h1 class="mb-8 mb-10 text-5xl font-black text-black">
			{{component.title}}</h1>
		<p class="mt-2 text-4xl text-black subtitle-feaures">{{component.subtitle}}</p>
		<div class="grid-feattures">
			{% for link in component.card_store %}
				<div class="flex flex-col items-center justify-center card-store-features">
					<div class="flex items-center justify-center rounded-full image">
						<img src="{{link.image}}"/>
					</div>
					<h2>{{ link.title_card }}</h2>
					<p>{{ link.subtitle_card }}</p>
				</div>
			{% endfor %}
		</div>
	</div>
</section>
